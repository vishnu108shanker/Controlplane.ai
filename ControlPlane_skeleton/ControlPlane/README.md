# ControlPlane.ai

**This is a placeholder. The real README — architecture summary, dependencies,
and execution instructions — is docs/tasks.md task 8, and is the last thing
that gets written, after the demo (`demo.py`) runs reliably end to end.**

Do not write the real README before that point: every claim and number in it
must come from code that actually runs in this repo (see
`docs/conventions.md`), so it can't be written ahead of the implementation
without becoming exactly the kind of invented-metric problem this project has
already rejected twice.

For now, see `docs/philosophy.md`, `docs/architecture.md`,
`docs/prerequisites.md`, `docs/conventions.md`, and `docs/tasks.md` for
everything an agent or developer needs to pick this up.
